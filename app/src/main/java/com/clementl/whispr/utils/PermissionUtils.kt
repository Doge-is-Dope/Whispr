package com.clementl.whispr.utils

import android.Manifest

// Permissions required for the main screen
val mainPermissions = listOf(
    Manifest.permission.CAMERA,
    Manifest.permission.RECORD_AUDIO
)